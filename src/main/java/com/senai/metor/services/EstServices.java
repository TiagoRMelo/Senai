package com.senai.metor.services;

import org.springframework.stereotype.Service;

@Service
public class EstServices {
   // @Autowired

   /*private FuncionarioRepository fRepository;
    public List<Funcionario> findALL(){
        return fRepository.findAll();

    }
    public Funcionario findById(Integer id){
        Optional<Funcionario> funcionario = fRepository.findById(id);
       return funcionario.orElseThrow(
        () -> new ObjectNotFoundException("Objeto ṇo encontrado! id:"+id+",Tipo:"+Funcionario.class.getName())
        
            
        

        
       );
    }
*/
}
